# makefile - WDK build system entry
# Standard WDK build makefile, points to the build system template.
# build.exe uses the sources file in the same dir. Keep pure ASCII.

!INCLUDE $(NTMAKEENV)\makefile.def
